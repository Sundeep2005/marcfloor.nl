# Marcfloor.nl

One-page React/Tailwind website for Marc Floor ten Brinke.

## Development

```bash
npm install
npm run dev -- --port 5173
```

## Artwin Live agenda

Copy `.env.example` to `.env` and replace `VITE_ARTWIN_WIDGET_URL` with the iframe URL from the Artwin Live website-widget generator.
